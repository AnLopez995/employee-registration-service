/**
 * Inbound REST adapter.
 *
 * <p>The package is {@code @NullMarked} (JSpecify): unless explicitly annotated,
 * no type accepts null. This is what allows honoring Spring's nullability
 * contracts, whose packages are null-marked as well.
 */
@NullMarked
package com.parameta.workforce.api.employee.infrastructure.rest;

import org.jspecify.annotations.NullMarked;
